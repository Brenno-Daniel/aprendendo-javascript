function exponenciacao(base, expoente) {
    const baseElevada = Math.pow(base, expoente)
    return console.log(`A base: ${base} elevada ao expoente: ${expoente} é: ${baseElevada}`)
}

exponenciacao(4, 0.5)